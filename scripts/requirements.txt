# Pinned for reproducibility.
# duckdb 1.2.2 is the minimum tested in CI (Ubuntu, Python 3.12).
# On Windows with Python 3.13 you may need 1.5.0+ due to a DLL linkage
# change; install manually with: pip install "duckdb>=1.5.0"
duckdb==1.2.2
pandas==2.2.3
pyarrow==19.0.1
requests==2.32.3
